server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abel-FL/configutations.git
          clone-on-start: true
          username: Abel-FL

logging:
  level:
    org.springframework.cloud.config: DEBUG
